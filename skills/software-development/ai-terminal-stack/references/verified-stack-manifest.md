# Verified Stack Manifest — user's Machine (2026-06-19 PM)

Single source of truth for what's actually installed. Verify via the check commands before any guide update.

## Core

| Component | Version/Binary | Verify With |
|-----------|---------------|-------------|
| Zellij | 0.44.3 | `zellij --version` |
| Neovim | 0.12.2 (Release, LuaJIT 2.1) | `nvim --version \| head -3` |
| Herm | via bun (~/.bun/bin/herm) | `which herm` |
| Shell | Git Bash (MSYS, POSIX) | — |

## Neovim

| Metric | Count | Verify With |
|--------|-------|-------------|
| Plugins (lazy) | 39 | `ls ~/AppData/Local/nvim-data/lazy/*/ \| wc -l` |
| LSP servers (Mason) | 9 distinct | `ls ~/AppData/Local/nvim-data/mason/bin/` |
| Treesitter parsers | 16 compiled | `nvim --headless -c "lua print(table.concat(require('nvim-treesitter').get_installed(), ', '))" -c "qa"` |

### Treesitter Parsers (16)
lua, vim, vimdoc, bash, python, javascript, typescript, go, rust, sql, markdown, yaml, json, jsx, ecma, markdown_inline

Compiler: MinGW-w64 GCC 16.1 via `vim.env.CC = "gcc"` in init.lua.

### Mason LSP Servers (9)
bash-language-server, lua-language-server, marksman, pyright, rust-analyzer, sql-language-server, typescript-language-server, vscode-json-language-server, yaml-language-server

(pyright-langserver.cmd also appears in mason/bin/ as a companion to pyright — not a separate server.)

### Complete Plugin List (39)
catppuccin, cmp_luasnip, cmp-nvim-lsp, Comment.nvim, conform.nvim, friendly-snippets, gitsigns.nvim, indent-blankline.nvim, lazy.nvim, lazygit.nvim, lualine.nvim, LuaSnip, markdown-preview.nvim, mason.nvim, mason-lspconfig.nvim, noice.nvim, nui.nvim, nvim-autopairs, nvim-cmp, nvim-lspconfig, nvim-notify, nvim-surround, nvim-treesitter, nvim-treesitter-textobjects, nvim-web-devicons, oil.nvim, plenary.nvim, readme, render-markdown.nvim, telescope.nvim, telescope-fzf-native.nvim, todo-comments.nvim, trouble.nvim, vim-dadbod, vim-dadbod-completion, vim-dadbod-ui, vim-fugitive, which-key.nvim, yazi.nvim

### Config Location
`~/AppData/Local/hermes\AppData\Local\nvim\init.lua` (378 lines)

### Clipboard
Native Windows provider (no win32yank):
- Copy: `clip.exe` (stdin → clipboard)
- Paste: `powershell.exe -NoProfile -Command Get-Clipboard -Raw`
- Set in `vim.g.clipboard` in init.lua

## Zellij

| Metric | Value |
|--------|-------|
| `clear-defaults` | `true` (completely custom bindings) |
| Layout files | `dev.kdl` (nvim 65% + herm 35%, uses forward-slash absolute paths) |
| Config location | `~/AppData/Local/hermes\AppData\Roaming\Zellij\config\config.kdl` |
| Layout location | `~/AppData/Local/hermes\AppData\Roaming\Zellij\config\layouts\dev.kdl` |

## Git

| Setting | Value |
|---------|-------|
| core.pager | delta |
| interactive.diffFilter | delta --color-only |
| delta.navigate | true |
| delta.side-by-side | true |
| merge.conflictstyle | diff3 |

## Compiler & Build Tools (MinGW-w64)

| Tool | Version | Location |
|------|---------|----------|
| gcc | 16.1.0 (MinGW-W64 x86_64-ucrt-posix-seh) | WinGet Packages\BrechtSanders.WinLibs.POSIX.UCRT\...\mingw64\bin\gcc.exe |
| cmake | 4.3.2 | Same dir as gcc |
| mingw32-make | 4.4.1 | Same dir as gcc |

## CLI Tools on PATH

| Tool | Location |
|------|----------|
| lazygit | WinGet Packages\JesseDuffield.lazygit\...\lazygit |
| fzf | WinGet Packages\junegunn.fzf\...\fzf |
| rg (ripgrep) | WinGet Packages\BurntSushi.ripgrep.MSVC\...\rg |
| btop4win | WinGet Packages\aristocratos.btop4win\...\btop4win |
| delta | WinGet Packages\dandavison.delta\...\delta |
| glow | WinGet Packages\charmbracelet.glow\...\glow |
| lazydocker | WinGet Packages\...\lazydocker |
| chezmoi | WinGet Packages\...\chezmoi |
| age | WinGet Packages\...\age |
| yazi | scoop\shims\yazi |
| sops | scoop\shims\sops |
| mdcat | scoop\shims\mdcat |
| http (httpie) | Python Scripts\http |
| docker | C:\Program Files\Docker\Docker\resources\bin\docker |
| node | C:\Program Files\nodejs\node |
| sqlite3 | WinGet Packages\SQLite.SQLite\...\sqlite3.exe |
| fd | WinGet Packages\sharkdp.fd\...\fd.exe |
| tree-sitter | WinGet Packages\tree-sitter.tree-sitter-cli\...\tree-sitter.exe |

## Shell Config

| File | Purpose |
|------|---------|
| `~/.bashrc` | Aliases (`v`→nvim, `zd`→dev layout, `lg`→lazygit, `y`→yazi), fzf+fd integration, custom functions (`fg`, `fgl`, `cheatsheet`), colored PS1 with git branch |
| `~/.bash_profile` | Sources .bashrc (auto-generated by Git for Windows) |
| `~/.fzf.key-bindings.bash` | Downloaded from fzf repo — Ctrl-T, Ctrl-R, Alt-C |
| `~/.fzf.completion.bash` | Downloaded from fzf repo — fuzzy completion |

## NOT Installed (notable gaps)
- ripgrep `rg` is installed (winget), but `ripgrep` command is not — use `rg`
- `btop` is not on PATH, but `btop4win` is
- No Go, Rust, Cargo, gh CLI, tmux
- No avante.nvim or claude-code configured yet
- No go LSP (gopls) — skipped because Go not installed
