# Project Dashboard Pattern

> **Superseded by the `project-dashboard-buildout` skill (wiki-planning category).**
>
> This reference was originally created alongside the brave-bookmarks-to-wiki skill as a sibling artifact. It has been consolidated into the dedicated `project-dashboard-buildout` skill which covers:
>
> - Graduation thresholds (when a topic needs a dashboard)
> - Full dashboard entity schema (in `dashboards/`, not `tracking/entities/`)
> - Migration pattern (tracking/ → dashboards/)
> - Bulk data augmentation (shopping table columns)
> - Data triage pattern (bookmark classification tiers)
> - Wiring into the dashboard system (index.md + system.md)
> - Both active projects' current state and buildout plans
>
> Load the skill via `skill_view(name='project-dashboard-buildout')` when working on dashboards.
